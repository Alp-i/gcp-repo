config {
  type: "table",
  schema: "core"
}
SELECT
  product_id,
  branch,
  COUNT(*) AS total_returns_count,
  SUM(quantity) AS total_quantity_returned,
  ROUND(SUM(quantity * price), 2) AS total_return_expense
FROM ${ref({schema:"staging" ,name:"stg_pubsub_returns"})}
GROUP BY product_id,branch

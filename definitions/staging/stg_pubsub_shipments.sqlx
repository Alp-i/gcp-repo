config {

    type: "table",
    schema: "staging",

}
SELECT shipment_id, carrier, status as durum, shipment_date, estimated_delivery_date FROM ${ref({schema:"landingzone", name:"pubsub_shipments"})}